import { defineConfig } from 'vitepress';

export default defineConfig({
    lang: 'zh-CN',
    title: 'Buig UI',
    description: 'Vue3 组件库（MVP 重构中）',
    srcDir: 'markdown',
    themeConfig: {
        nav: [],
        sidebar: []
    }
});
